using UnityEngine;

public class PositionOnStart : MonoBehaviour {

    void Awake()
    {
        //var tap = gameObject.GetComponent<TapToPlaceParent>();
        //if (tap != null)
        //    tap.OnSelect();
    }
}
